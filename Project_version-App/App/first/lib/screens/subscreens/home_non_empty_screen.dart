import 'package:flutter/material.dart';

class HomeNonEmptyScreen extends StatelessWidget {
  const HomeNonEmptyScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Burada durak listesi görünecek."),
    );
  }
}
